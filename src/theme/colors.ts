import {palette} from './palette';

export const colors = {
  mainBackground: palette.white,
  buttonPrimaryBackground: palette.bluePrimary,
  cardPrimaryBackground: palette.purplePrimary,
  headerColor: palette.headerColor,
  bodyText: palette.blackLight,
};
